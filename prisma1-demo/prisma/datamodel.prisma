type Pokemon {
  id: ID! @id @unique
  number: Int! @unique
  name: String!
  attacks: PokemonAttack
}

type PokemonAttack {
  id: Int! @id
  special: [Attack]
}

type Attack {
  id: Int! @id
  name: String
  damage: String
}
