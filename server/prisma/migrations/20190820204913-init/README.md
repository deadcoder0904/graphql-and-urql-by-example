# Migration `20190820204913-init`

This migration has been generated by Akshay Kadam at 8/20/2019, 8:49:13 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Pokemon"("id" TEXT NOT NULL  ,"number" INTEGER NOT NULL DEFAULT 0 ,"name" TEXT NOT NULL DEFAULT '' ,"attacks" INTEGER   REFERENCES "PokemonAttack"(id) ON DELETE SET NULL,PRIMARY KEY ("id"))
;

CREATE TABLE "lift"."PokemonAttack"("id" INTEGER NOT NULL DEFAULT 0 ,PRIMARY KEY ("id"))
;

CREATE TABLE "lift"."Attack"("id" INTEGER NOT NULL DEFAULT 0 ,"name" TEXT NOT NULL DEFAULT '' ,"damage" TEXT NOT NULL DEFAULT '' ,"pokemonAttack" INTEGER   REFERENCES "PokemonAttack"(id) ON DELETE SET NULL,PRIMARY KEY ("id"))
;

CREATE UNIQUE INDEX "lift"."Pokemon.id._UNIQUE" ON "Pokemon"("id")

CREATE UNIQUE INDEX "lift"."Pokemon.number._UNIQUE" ON "Pokemon"("number")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190820204913-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,31 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+}
+
+model Pokemon {
+  id      String         @default(cuid()) @id @unique
+  number  Int            @unique
+  name    String
+  attacks PokemonAttack?
+}
+
+model PokemonAttack {
+  id      Int      @id
+  special Attack[]
+}
+
+model Attack {
+  id     Int    @id
+  name   String
+  damage String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190820204913-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190820204913-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
